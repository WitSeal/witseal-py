"""CLI entry point — placeholder until Layer 5."""

from __future__ import annotations

import sys


def main() -> int:
    sys.stderr.write(
        "witseal: CLI is not yet implemented in the Python track (v0.0.0 placeholder).\n"
        "See https://github.com/WitSeal/witseal for the TypeScript reference CLI.\n"
    )
    return 1


if __name__ == "__main__":
    raise SystemExit(main())
